import { HighlightNewestDirective } from './highlight-newest.directive';

describe('HighlightNewestDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightNewestDirective();
    expect(directive).toBeTruthy();
  });
});
